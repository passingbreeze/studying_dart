void main(List<String> args) {
  // compile-time constant -> const, 컴파일 시점에는 반드시 알아야하는 상수
  const name = 'hongjeongmin';
  // late const lastname = 'hong'; -> late 사용할 수 없다.
}

/**
 * Variable Naming Convention
 * 
 * _, $, alphabet, number only available
 * its name cannot begin with numbers
 * 
 */